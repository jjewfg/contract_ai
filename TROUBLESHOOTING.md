\# TROUBLESHOOTING · 排错记录



两个项目的工程排错档案，按"现象 → 根因 → 修复"归档，持续追加。

覆盖项目：law-assistant（劳动法 RAG 问答）、contract-extractor（合同信息抽取）。



\## 一、环境



\- \*\*Gradio 报 unexpected keyword argument 'theme'\*\*：本地 gradio 过旧。升级 `pip install -U gradio` 或删参数。

\- \*\*python 跑到系统 Python 而非 .venv\*\*：新终端未激活。先 `.venv\\Scripts\\Activate.ps1`，确认提示符带 (.venv)。

\- \*\*端口占用 Errno 10048\*\*：旧进程残留。`netstat -ano | findstr :7860` 查 PID → `taskkill /PID xxx /F`。

\- \*\*Gradio 启动 502（startup-events failed）\*\*：代理软件劫持 localhost。设 `$env:NO\_PROXY="localhost,127.0.0.1"` 或代理白名单。



\## 二、文件与编码



\- \*\*Dockerfile 变成 Dockerfile.txt\*\*：记事本强加后缀，docker build 找不到文件。保存类型选"所有文件"，建完 `dir` 验证。

\- \*\*脚本零输出、秒退、无报错\*\*：文件粘贴不完整（只剩注释时 Python 直接跳过）。建文件后必做 `Get-Content xxx.py -TotalCount 3` + `ast.parse` 语法验证。

\- \*\*unterminated string literal\*\*：复制时换行符损坏。代码内换行统一用 `NL = chr(10)` 生成。

\- \*\*PowerShell 中文乱码\*\*：GBK 终端显示 UTF-8 文件，仅显示问题。`chcp 65001` 或 `Get-Content -Encoding UTF8`。

\- \*\*手改 JSON 后评测离奇崩坏\*\*：逗号引号极易改伤。每次手改后必跑 `json.load` 校验。



\## 三、网络与部署



\- \*\*浏览器开 0.0.0.0 报 ERR\_ADDRESS\_INVALID\*\*：0.0.0.0 是监听地址不是访问地址，访问用 `http://127.0.0.1:7860`。

\- \*\*魔搭空间"运行错误"\*\*：API Key 环境变量未配。密文管理添加后必须点"重新部署"。

\- \*\*GitHub Actions 不触发\*\*：`.github` 文件夹网页拖拽上传丢失。改用网页编辑器直接新建 `.github/workflows/ci.yml`。



\## 四、评测方法论（本项目评测迭代 76.9% → 96.9% 的核心教训）



\- \*\*某字段系统性归零（0/20）\*\*：先怀疑评测器再怀疑模型。本案标准答案是完整条款、模型输出枚举值，精确匹配永远为假 → 改关键词判定。

\- \*\*整份 FAIL 连坐 8 字段\*\*：模型对缺失字段输出 null，Pydantic 必填校验炸全份 → 解析后统一清洗 `null → ""`，缺字段只扣该字段。

\- \*\*标签与正文漂移\*\*：合成数据管线的"要素清单（草稿）"被 LLM 写正文时合理化改写（如倒挂日期被自动修正）→ ground truth 必须以正文为准，逐字转录；错误集中在同一模板类型是模板性 bug 的旁证。

\- \*\*评测采样噪声\*\*：temperature=0.1 下同一输入跨轮次结果漂移（时对时错）→ 评测必须 `temperature=0`，否则一切归因都是猜谜。

\- \*\*字段规格模糊\*\*：只写"起始日期"不写"什么的日期"，模型在多种候选（交货期/生效日/付款截止）间摇摆 → Prompt 写死定义 + 边界规则（仅有"X日前"式截止日期 → 填 null）。

\- \*\*gold 为空被冤判\*\*：正文确实没写某字段时 gold 置空，模型如实输出空应判对 → 评测器补"空对空 = PASS"规则（gold\_kong）。

\- \*\*borderline 样本 + 敏感指标 = 分数打地鼠\*\*：penalty 的对称相似度 0.40\~0.47 整齐卡在 0.5 阈值下，5 份样本语义全对却判错——逐字条款原文 vs 要素摘要的长度差被对称比对惩罚 → 指标局限，登记已知限制；v4 用覆盖度（containment）+ 金额归一化在留出集上重新标定。



\## 五、评测纪律（血案换来的原则）



1\. 评测必须 temperature=0——不可复现的评测，任何归因都是猜谜。

2\. 系统性归零先怀疑裁判，再怀疑考生。

3\. 标准答案以成品（正文）为准，草稿（要素清单）不可信。

4\. Prompt 字段必须定义清楚："起始日期"不行，"履行期间的起始日期"才行。

5\. 单变量纪律：一次只改一处，改完立刻测；中途态（如 77.5% 事件）不可归因，不回溯追查。

6\. 三方对照（标签 vs 模型输出 vs 正文原文）是归因神器——先跑对照，再下结论。

7\. 不在测试集上调整阈值/裁判规则——看成绩改评分线 = 过拟合评测器；修"可证明失效"的规则（如精确匹配枚举值必错）不在此列，判定标准：该修改在看分数之前是否站得住。



\## 案例研究：一场评测"打地鼠"战役（76.9% → 96.9%）



| 轮次 | 总分 | 当时以为的病 | 真正的病 |

|---|---|---|---|

| v1 | 76.9% | 模型差 | 裁判冤案（0/20）+ null 连坐（2 份 FAIL） |

| v2 | 96.2% | 剩 4 个小错 | 标签抄草稿 + 温度噪声 |

| 中途 | 77.5% | 恐慌 | 多处修改混改的中途态，不可归因 |

| 复测 | 95.0% | 02 怎么又错了 | temperature=0.1 的假失败 |

| 三方诊断 | — | — | 噪声 + 规格模糊 + gold 该为空 |

| v3 封版 | 96.9% | — | 7/8 字段 100%；penalty 75% 为指标局限，登记已知限制 |



工具沉淀：`diagnose.py` / `diagnose2.py`（三方对照 + 相似度打印），任何评测异常先用它定位，再动代码。



