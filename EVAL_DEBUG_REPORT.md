notepad EVAL\_DEBUG\_REPORT.md

``````markdown

\# 评测调试报告：从 76.9% 到 96.9% 的一次完整迭代



> 项目：合同智能抽取助手（contract-extractor）

> 日期：2026-08-30

> 模型：DeepSeek-V3 ｜ 评测集：20 份合成合同 × 8 字段

> 一句话结论：首轮 76.9% 的低分中，约一半来自评测系统自身的设计缺陷而非模型能力；经五处修复（2 处代码、2 处标签、1 处评估配置）并确立温度归零的可复现性后，7/8 字段达到 100%，总体 96.9%；唯一未满分字段经逐份人工核对确认全部为指标局限而非抽取错误，如实登记为已知限制。



\## 一、评测体系背景



抽取管线：合同正文 → LLM 抽取（JSON）→ Pydantic 校验（失败重试 1 次）→ 结构化输出。



评测集由 gen\_data.py 合成：先随机生成"要素画像"（甲乙方、金额、起止日期、违约条款、争议解决方式），再让 LLM 按画像撰写 600 字合同正文，画像同时作为评测的 ground truth 存入 labels.json。共 20 份、四种类型轮转（劳动 / 房租 / 买卖 / 服务）。



评分规则按字段特性分级设计：枚举与名称字段精确匹配；金额按数字集合比对；违约条款按字符相似度（阈值 0.5）；争议解决按关键词判定。



这个"按字段特性设计匹配策略"的框架本身是对的，问题出在细节实现上——这正是本次迭代的全部剧情。



\## 二、迭代时间线总览



| 轮次 | 总体准确率 | 当时以为的病 | 真正的病 |

|---|---|---|---|

| v1（11:21） | 76.9% | 模型能力差 | 评测器冤案（某字段 0/20）+ null 连坐（2 份 FAIL） |

| v2（11:29） | 96.2% | 只剩 4 个小错 | 标签抄了草稿 + 温度采样噪声 |

| 中途态 | 77.5% | 数据损坏恐慌 | 手改 labels.json 的中间状态，按单变量纪律不回溯归因 |

| 复测（19:02） | 95.0% | 02 怎么又错了 | temperature=0.1 的采样噪声假失败 |

| 三方诊断 | — | — | 噪声 + 字段规格模糊 + gold 本应为空 |

| v3（21:08） | \*\*96.9%（封版）\*\* | — | 7/8 字段 100%；penalty 75% 为指标局限，登记已知限制 |



\## 三、逐轮推理与修复



\### v1：76.9% —— 两个结构性缺陷同时显形



\*\*异常一：dispute\_resolution 字段 0/20。\*\*

推理：20 份样本全部错误同一字段，属于"系统性归零"。模型再差也不至于 20 份全错同一个字段——先怀疑裁判，再怀疑考生。

核查：标准答案存储的是完整条款原文（"协商不成的，向合同签订地人民法院提起诉讼"），而 Prompt 要求模型输出枚举值（"诉讼 或 仲裁"），评分却做全字符串精确比对——两个字对一整段话，数学上永远为假。任何模型在此规则下必然 0 分：这把尺子根本没量到要量的东西。

修复：该字段改为关键词判定（gold 含"仲裁"则要求 pred 含"仲裁"，含"诉讼/法院"则要求含"诉讼"）。语义等价即得分。



\*\*异常二：contract\_02 / contract\_10 整份 FAIL（8 字段连坐）。\*\*

报错：Pydantic "2 validation errors, term\_start: Input should be a valid string"。

推理：模型按 Prompt 规则对缺失字段输出了 null；而数据模型声明的是 `term\_start: str = ""`——Pydantic 中"字段缺省"与"字段显式为 null"是两回事，null 与 str 类型校验直接失败，整个 JSON 作废，8 字段连坐。

修复：JSON 解析后、校验前，加一道清洗 `{k: ("" if v is None else v)}`，null 统一洗成空串。缺失字段从此只扣该字段分，不再引爆全份。



v2 结果：96.2%，0 份 FAIL，dispute\_resolution 100%。



\### 深挖：剩余 4 个错误的三方对照



对失败样本跑单份抽取（后固化为 diagnose.py：标签 vs 模型输出 vs 正文原文，三方并列打印），发现：



\- contract\_06：模型抽出的日期与正文逐字一致且格式合法，却被判错 → 唯一可能是标签与正文不符。

\- 根因：合成管线的买卖合同模板中，起止月为两个独立随机数，可摇出"2026-11-10 至 2026-02-20"式倒挂期间；LLM 按画像撰写正文时自动将荒谬日期合理化改写——画像（草稿）与正文（成品）就此漂移，而 labels 抄的是草稿。

\- 原则确立：\*\*ground truth 以正文为准\*\*（评测目标是"从正文抽对字段"，正文才是唯一信息源）。



修正标签的过程中出现了两次宝贵的"现场事故"，如实记录：



\*\*事故一（77.5% 中途态）\*\*：多处手工修改未及时逐一验证，产生一个无法归因的中间分数。教训固化为\*\*单变量纪律\*\*：一次只改一处，改完立刻测；中途态不回溯追查（文件已被健康状态覆盖，证据消失，追了无益）。



\*\*事故二（误伤 02）\*\*：曾凭直觉推断"02 的倒挂日期被顺手改合理导致回归错误"。三方对照推翻了这一假设：02 的标签、模型输出、正文三方完全一致，它在 19:02 轮的失败是\*\*温度采样噪声\*\*。教训：先跑对照，再下结论——直觉在证据面前一律让位。



\### v3：三处根因同时修复



\*\*根因一：temperature=0.1 的采样噪声。\*\* 同一合同在四轮评测中结果漂移（06 在 v1 抽对、v2/诊断轮输出 null；02 在复测轮假失败、诊断轮正常）。0.1 不是 0——模型在模糊地带会掷骰子，不可复现的评测让一切归因变成猜谜。修复：temperature=0，从此每轮数字可复现。



\*\*根因二：字段规格模糊。\*\* Prompt 原来只写"term\_start（起始日期）"，未定义"是什么的日期"。合同里同时存在交货期、生效日、付款截止日多种日期候选，模型在"映射履行期间"与"判 null"之间摇摆。修复：规格写死——"合同履行期间的起始日期（交货期/服务期/租期的开始日）"；并加边界规则："正文仅出现『某日前』式单独截止日期而无明确起止期间时，term\_start 与 term\_end 均填 null"。



\*\*根因三：gold 可为空未定义。\*\* contract\_10 正文只有两个独立截止日期（交付截止、付款截止），无任何起止期间——原标签是生成器把两个截止日硬凑成的"期间"，正文不支持，gold 应置空。但评测器规则"pred 为空即判错"会把"模型如实输出 null"冤判为错。修复：评测器补"空对空 = PASS"规则（gold\_kong）：标准答案为空时，模型输出也为空即得分。



v3 结果：\*\*96.9%（155/160），7/8 字段 100%\*\*。



\### penalty 字段：96.9% 的缺口归因（diagnose2.py）



温度归零后 penalty 从 90% 变为 75%（新增 3 份失败），必须查清是倒退还是别的什么。



对 5 份失败样本（02/07/10/11/15）逐份人工核对，结论一致：\*\*全部语义正确，且比标签更忠实\*\*——标签是要素摘要（"延迟交付的，每延迟一日按合同总价的千分之三支付违约金"），模型按 Prompt 要求摘录条款原文（常附带解约权、损失赔偿等附加内容，并将"百分之十"规范为"10%（即 73000 元）"）。5 份的对称相似度整齐落在 0.40\~0.47，全部卡在 0.5 阈值之下。



定性：\*\*指标局限，而非模型错误\*\*。对称字符相似度（SequenceMatcher ratio）天然惩罚"逐字原文 vs 要素摘要"的长度差；温度归零带来的输出风格微调，把三份原本压在线上的 borderline 样本推到了线下——这正是"borderline 样本 + 敏感指标 = 分数打地鼠"的典型签名（penalty 三轮分别为 75% → 90% → 75%，而正文一个字没变过）。



处置（评测纪律）：\*\*不在测试集上调整阈值\*\*——看着成绩单挪及格线等于过拟合评测器。修复"可证明失效"的规则（如 v1 的精确匹配冤案）与为救特定样本调参数，判定标准是一条：\*\*该修改在看分数之前是否站得住\*\*。此处置不满足。如实登记为已知限制，指标升级列入 v4 计划。



\## 四、最终状态



| 字段 | 准确率 | 备注 |

|---|---|---|

| contract\_type / party\_a / party\_b / amount / dispute\_resolution | 100% | |

| term\_start / term\_end | 100% | 依赖规格明确化 + gold 置空（10） |

| penalty | 75% | 指标局限，已知限制；v4 用覆盖度（containment）+ 金额归一化在扩充后的留出集上重新标定 |

| \*\*总体\*\* | \*\*96.9%\*\* | temperature=0，每轮可复现 |



\## 五、方法论沉淀（血案换来的七条）



1\. 评测必须 temperature=0——不可复现的评测，任何归因都是猜谜。

2\. 某字段系统性归零（0/20），先怀疑裁判，再怀疑考生。

3\. 标准答案以成品（正文）为准，草稿（要素清单）不可信；合成数据管线应在生成时加逻辑约束、生成后做回环校验。

4\. Prompt 中字段必须定义完整："起始日期"不行，"履行期间的起始日期"才行；边界情况（缺失、仅截止日期）要预先规定输出规则。

5\. 单变量纪律：一次只改一处，改完立刻测；中途态分数不可归因，不回溯追查。

6\. 三方对照（标签 vs 模型输出 vs 正文原文）是归因第一工具——先跑对照，再下结论；直觉假设被证据推翻时公开修正。

7\. 不在测试集上调整裁判规则与阈值；区分"修可证明失效的规则"与"看着分数挪及格线"。



\## 六、附录：归因工具



\- `diagnose.py`：指定合同的三方对照（标签 / 模型抽取 / 正文含年份行），定位"谁错了"。

\- `diagnose2.py`：penalty 专项（标签、抽取、相似度三元组），定位"错得有多冤"。

\- `eval\_report2.md`：封版评测报告（96.9%，temperature=0）。



